<Window x:Class="EuroPromotionProject.ClientInformationWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EuroPromotionProject"
        mc:Ignorable="d" Title="Καταχώρηση στοιχεία πελατών" 
        Width="auto"
        Height="auto" 
        WindowStartupLocation="CenterScreen" Closed="Window_Closed">
    <Border BorderBrush="#E2E8F0" BorderThickness="1">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop x:Name="back1" Offset="0.0" Color="#00CCCC" />
                <GradientStop x:Name="back2" Offset="0.5" Color="#fff" />
                <GradientStop x:Name="back3" Offset="1.0" Color="#00CCCC" />
            </LinearGradientBrush>
        </Border.Background>

        <Grid SnapsToDevicePixels="True">

            <Grid.Resources>
                <SolidColorBrush x:Key="Primary" Color="#3B82F6" />

                <Style x:Key="BtnPrimary" TargetType="Button">
                    <Setter Property="Height" Value="45" />
                    <Setter Property="Background" Value="{StaticResource Primary}" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="FontSize" Value="16" />
                    <Setter Property="Cursor" Value="Hand" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="10">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Opacity" Value="0.9" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Grid.Resources>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Height="70" Padding="20,0" CornerRadius="0,0,15,15">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop x:Name="Stop1" Offset="0.0" Color="#53DDDD" />
                        <GradientStop x:Name="Stop2" Offset="0.5" Color="#0066CC" />
                        <GradientStop x:Name="Stop3" Offset="1.0" Color="#660099" />
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock FontSize="22" FontWeight="Bold" Foreground="White" Text="EuroPromoter" />
                    <TextBlock Margin="10,0,0,0" VerticalAlignment="Bottom" FontSize="14" Foreground="White" Opacity="0.8" Text="Στοιχεία Πελάτη" />
                </StackPanel>
            </Border>

            <ScrollViewer Grid.Row="1" Padding="20" CanContentScroll="False" PanningMode="Both" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Padding="20" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="15">
                        <StackPanel>
                            <StackPanel.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontSize" Value="12" />
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                    <Setter Property="Foreground" Value="#64748B" />
                                    <Setter Property="Margin" Value="2,0,0,5" />
                                </Style>

                                <Style TargetType="TextBox">
                                    <Setter Property="Height" Value="40" />
                                    <Setter Property="Background" Value="#F8FAFC" />
                                    <Setter Property="BorderBrush" Value="#E2E8F0" />
                                    <Setter Property="BorderThickness" Value="1" />
                                    <Setter Property="Padding" Value="10,0" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="Margin" Value="0,0,0,15" />
                                    <Setter Property="FontSize" Value="14" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="TextBox">
                                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                                                    <ScrollViewer x:Name="PART_ContentHost" />
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>

                                <Style TargetType="ComboBox">
                                    <Setter Property="Height" Value="40" />
                                    <Setter Property="Margin" Value="0,0,0,15" />
                                    <Setter Property="FontSize" Value="14" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBox">
                                                <Grid>
                                                    <ToggleButton x:Name="ToggleButton" Grid.ColumnSpan="2" ClickMode="Press" Focusable="false" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                                        <ToggleButton.Template>
                                                            <ControlTemplate TargetType="ToggleButton">
                                                                <Border x:Name="templateBorder" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8">
                                                                    <Grid>
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="*" />
                                                                            <ColumnDefinition Width="30" />
                                                                        </Grid.ColumnDefinitions>
                                                                        <Path Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0" Stroke="#64748B" StrokeThickness="2" />
                                                                    </Grid>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="true">
                                                                        <Setter TargetName="templateBorder" Property="BorderBrush" Value="#2563EB" />
                                                                    </Trigger>
                                                                    <Trigger Property="IsChecked" Value="true">
                                                                        <Setter TargetName="templateBorder" Property="BorderBrush" Value="#2563EB" />
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </ToggleButton.Template>
                                                    </ToggleButton>

                                                    <ContentPresenter x:Name="ContentSite" Margin="12,0,30,0" HorizontalAlignment="Left" VerticalAlignment="Center" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" IsHitTestVisible="False" />

                                                    <Popup x:Name="PART_Popup" AllowsTransparency="True" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" Placement="Bottom">
                                                        <Border x:Name="DropDownBorder" MinWidth="{TemplateBinding ActualWidth}" Margin="0,4,0,0" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8">
                                                            <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                                                <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                                            </ScrollViewer>
                                                        </Border>
                                                    </Popup>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>

                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="Padding" Value="10,10" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBoxItem">
                                                <Border x:Name="ItemBorder" Margin="2" Background="Transparent" CornerRadius="4">
                                                    <ContentPresenter />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ItemBorder" Property="Background" Value="#F1F5F9" />
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="ItemBorder" Property="Background" Value="#DBEAFE" />
                                                        <Setter Property="Foreground" Value="#2563EB" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </StackPanel.Resources>

                            <TextBlock Margin="0,0,0,20" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold" Foreground="#1E293B" Text="Στοιχεία Επικοινωνίας" />
                            <TextBlock Text="1. Promoter*" />
                            <Grid Margin="5">
                                
                                <ComboBox x:Name="ComboPromoter" Height="40" Margin="0,0,0,15" SelectionChanged="PromoterCombo_SelectionChanged" Style="{StaticResource ComboBoxStyle}">
                                </ComboBox>
                                <TextBlock Name="ComboPromoterPlaceHolder" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,10" Foreground="Black" FontSize="16" IsHitTestVisible="False">--ΕΠΙΛΕΞΤΕ PROMOTER--</TextBlock>

                            </Grid>
                            <TextBlock Text="2. Επωνυμία Φαρμακείου *" />
                            <TextBox x:Name="TxtPharmacy" CharacterCasing="Upper" PreviewKeyDown="TxtPharmacy_PreviewKeyDown"/>

                            <TextBlock Text="3. Πόλη" />
                            <TextBox x:Name="TxtCity" CharacterCasing="Upper" PreviewKeyDown="TxtCity_PreviewKeyDown"/>

                            <TextBlock Text="4. Τηλέφωνο *" />
                            <TextBox x:Name="TxtPhone" MaxLength="10" PreviewTextInput="TxtPhone_PreviewTextInput" PreviewKeyDown="TxtPhone_PreviewKeyDown"/>

                            <TextBlock Text="5. e-mail" />
                            <TextBox x:Name="TxtEmail" />

                            <TextBlock Text="6. Πρόγραμμα" />
                            <ComboBox x:Name="ComboProgram" Height="40" Margin="0,0,0,15" SelectionChanged="ProgramCombo_SelectionChanged" />

                            <TextBlock x:Name="TxtOtherProgramBlock" Text="Προσδιορίστε το Πρόγραμμα:" Visibility="Collapsed">
                            </TextBlock>
                            <TextBox  x:Name="TxtOtherProgram" CharacterCasing="Upper" Visibility="Collapsed"/>
                            <!--<TextBox x:Name="TxtOtherProgram" CharacterCasing="Upper">
                                <TextBox.Style>
                                    <Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="TextBox">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            --><!-- Εμφάνιση μόνο όταν η επιλογή είναι "ΑΛΛΟ" --><!--
                                            <DataTrigger Binding="{Binding ElementName=ComboProgram, Path=SelectedItem}" Value="ΑΛΛΟ">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>-->

                            <TextBlock Text="7. Πελάτης" />
                            <ComboBox x:Name="ComboClient" Height="40" Margin="0,0,0,15" SelectionChanged="ClientCombo_SelectionChanged" />


                            <TextBlock Text="8. PRESENTATION" />
                            <Grid Margin="5">
                                <ComboBox x:Name="ComboPresentation" Height="40" Margin="0,0,0,15" SelectionChanged="PresentationCombo_SelectionChanged" Style="{StaticResource ComboBoxStyle}">
                                </ComboBox>
                                <TextBlock Name="ComboPresenterPlaceHolder" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,10" Foreground="Black" FontSize="16" IsHitTestVisible="False">--ΕΠΙΛΕΞΤΕ PRESENTER--</TextBlock>
                            </Grid>
                            <TextBlock Text="9. SALES" />
                            <Grid Margin="5">
                                <ComboBox x:Name="ComboSales" Height="40" Margin="0,0,0,15" SelectionChanged="SalesPersonCombo_SelectionChanged" Style="{StaticResource ComboBoxStyle}">
                                </ComboBox>
                                <TextBlock Name="ComboSalesPlaceHolder" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,10" Foreground="Black" FontSize="16" IsHitTestVisible="False">--ΕΠΙΛΕΞΤΕ SALES--</TextBlock>
                            </Grid>
                            <TextBlock Text="10. Παρατηρήσεις" />
                            <WrapPanel Margin="0,0,0,5">
                                <Button Margin="0,0,5,5" Padding="8,3" Background="#F1F5F9" Click="QuickNote_Click" Content="+ Ήρθε ο/η βοηθός" />
                                <Button Margin="0,0,5,5" Padding="8,3" Background="#F1F5F9" Click="QuickNote_Click" Content="+ Θα ανοίξει φαρμακείο" />
                                <Button Margin="0,0,5,5" Padding="8,3" Background="#F1F5F9" Click="QuickNote_Click" Content="+ Έχει άλλο πρόγραμμα, είναι ευχαριστημένος" />
                                <Button Margin="0,0,5,5" Padding="8,3" Background="#F1F5F9" Click="QuickNote_Click" Content="+ Έχει άλλο πρόγραμμα αλλά θέλει να αλλάξει" />
                                <Button Margin="0,0,5,5" Padding="8,3" Background="#F1F5F9" Click="QuickNote_Click" Content="+ Να καλέσουμε για παρουσίαση" />
                                <Button Margin="0,0,5,5" Padding="8,3" Background="#F1F5F9" Click="QuickNote_Click" Content="+ Να καλέσουμε για ραντεβού εγκατάστασης" />
                                <Button MaxWidth="250" Margin="0,0,5,5" Padding="8,3" Background="#F1F5F9" Click="QuickNote_Click">
                                    <TextBlock Text="+ Πήρε φυλλάδιο και ενημερώθηκε για την προσφορά της έκθεσης" TextAlignment="Center" TextWrapping="Wrap" />
                                </Button>
                            </WrapPanel>

                            <TextBox x:Name="TxtNotes" Height="80" VerticalContentAlignment="Top" AcceptsReturn="True" TextWrapping="Wrap" />

                            <StackPanel x:Name="signPanel" Visibility="Visible" Grid.Row="1" Margin="30">

                              

                                <TextBlock Margin="5,0,0,10" FontWeight="SemiBold" Foreground="#475569" Text="Παρακαλώ υπογράψτε στο παρακάτω πλαίσιο:" />
                                <Border Height="250" Background="White" BorderBrush="#CBD5E1" BorderThickness="2" CornerRadius="12">
                                    <Grid>
                                        <InkCanvas x:Name="SignCanvas" Margin="5" Background="Transparent" />
                                        <Line Margin="10,0,10,30" VerticalAlignment="Bottom" HorizontalAlignment="Center" Stroke="#E2E8F0" StrokeThickness="1" X1="0" X2="700" Y1="230" Y2="230" />
                                    </Grid>
                                </Border>
                                
                                <Border Margin="0,0,0,20" Padding="15" Background="White" CornerRadius="10">
                                    <CheckBox x:Name="ChkConsent" VerticalContentAlignment="Top">
                                        <TextBlock FontSize="13" FontWeight="Bold" Foreground="#334155" LineHeight="18" Text="ΣΥΝΑΙΝΩ για την τήρηση των προσωπικών μου στοιχείων απο τη Europharmacy ΙΚΕ για μελλοντική επικοινωνία με σκοπό την ενημέρωσή μου." TextWrapping="Wrap" />
                                    </CheckBox>
                                </Border>


                            </StackPanel>
                            <Button Click="BtnFinish_Click" Content="Ολοκλήρωση &amp; Επιβεβαίωση" Style="{StaticResource BtnPrimary}" />

                        </StackPanel>
                    </Border>
                    <TextBlock Margin="5,15,5,5" FontSize="11" FontStyle="Italic" Foreground="#64748B" Text="* Στην περίπτωση που επιθυμείτε να διαγραφούν τα προσωπικά σας στοιχεία από τη βάση δεδομένων της Europharmacy μπορείτε να ανακαλέσετε εγγράφως τη συγκατάθεσή σας ανά πάσα στιγμή." TextWrapping="Wrap" />
                    <TextBlock Margin="5" HorizontalAlignment="Left" FontSize="11" Foreground="#64748B" Text="* Τα πεδία είναι υποχρεωτικά" />

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
